{
  "name": "donation-widget",
  "version": "1.0.0",
  "description": "A simple embeddable donation widget using Stripe Elements for political campaigns",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "start:host": "node server/server.js --host",
    "start:preview": "node server/server.js --preview",
    "start:full": "node server/server.js --host --preview",
    "dev": "nodemon server/server.js",
    "dev:host": "nodemon server/server.js --host",
    "dev:preview": "nodemon server/server.js --preview",
    "dev:full": "nodemon server/server.js --host --preview",
    "dev:ui": "vite",
    "build:ui": "vite build",
    "preview:ui": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "donation",
    "widget",
    "stripe",
    "political",
    "campaign",
    "fundraising"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "stripe": "^14.9.0",
    "dotenv": "^16.3.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "vite": "^7.1.7",
    "@vitejs/plugin-react": "^5.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
